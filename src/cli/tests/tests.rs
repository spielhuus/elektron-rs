mod tests {
    mod cli {
        /* use super::library;
        use crate::sexp::SexpValueQuery;

        #[test]
        fn load_symbols() {
            let lib = library(
                "Amplifier_Operational:AD8015",
                vec![
                    String::from("files/symbols"),
                    String::from("files/other_symbols"),
                ],
            );
            assert!(lib.is_ok());
            let name: String = lib.unwrap().get(0).unwrap();
            assert_eq!(String::from("Amplifier_Operational:AD8015"), name);
        } */
    }
}
